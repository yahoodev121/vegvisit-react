query getMoreThreadItems($offset: Int, $threadId: Int!){
  getAllThreadItems(offset: $offset, threadId: $threadId) {
    id
    threadId
    reservationId
    content
    sentBy
    type
    messageType
    startDate
    endDate
    createdAt
    status
    isRead
  }
}